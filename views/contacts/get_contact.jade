extends ../layout

block content
  h1 Checked-Ins
  p Hello, #{User}
  a(href='/home') Home
  br
  br
  ul
    li
      each person in contact.contact
        p
          a(href="/contacts/#{contact._id}")= 'Edit'
          ,#{person.name}, #{person.phone}, #{person.email}
